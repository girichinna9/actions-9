name: My First Workflow

on: push

jobs: 
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: checkout repository
          uses: actions/checkout@v4
          
        - name: Welcome Message, list and read readme file
          run: |
            echo "My first guthub actions job"
            ls -ltra
            cat README.md
        
        - name: Generate ASCII Artwork
          run: cowsay -f cat "Run for cover, I am cat....meow" >> cat.txt

        # - name: List Files
        #   run: ls
            
        # - name: Read File
        #   run : cat README.md